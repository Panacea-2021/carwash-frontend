# Babacarwash Admin

Vite + React admin dashboard for Babacarwash.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
```

## Environment

Copy `.env.example` to `.env`.

- Local API default: `http://localhost:3002/api`
- Production uses `/api`, which Vercel proxies to the live backend.
